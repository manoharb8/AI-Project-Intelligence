import type {ReactNode} from 'react';
import {FileText,LoaderCircle,AlertCircle} from 'lucide-react';
import type {ProjectDocument} from '../types';
export function PageHeading({eyebrow,title,description,action}:{eyebrow:string;title:string;description:string;action?:ReactNode}){return <div className="page-heading"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="muted">{description}</p></div>{action}</div>}
export function ErrorNotice({children}:{children:ReactNode}){return <div className="notice error" role="alert"><AlertCircle size={18}/><span>{children}</span></div>}
export function Busy({children}:{children:ReactNode}){return <div className="notice" role="status"><LoaderCircle className="spin" size={18}/>{children}</div>}
export function Empty({title,children}:{title:string;children:ReactNode}){return <div className="empty"><FileText size={28}/><h3>{title}</h3><p>{children}</p></div>}
export function DocumentTable({documents}:{documents:ProjectDocument[]}) {return documents.length?<div className="table-scroll"><table><thead><tr><th>Source document</th><th>Format</th><th>Status</th><th>Passages</th><th>Uploaded</th></tr></thead><tbody>{[...documents].reverse().map(doc=><tr key={doc.id}><td><div className="filename"><FileText size={19}/><div><strong>{doc.filename}</strong><small>{(doc.size_bytes/1024).toFixed(1)} KB</small>{doc.error&&<span className="file-error">{doc.error}</span>}</div></div></td><td><span className="format">{doc.document_type.toUpperCase()||'UNKNOWN'}</span></td><td><span className={`badge ${doc.status}`}>{doc.status==='indexed'?'Indexed':doc.status==='error'?'Failed':'Processing'}</span></td><td>{doc.chunk_count}</td><td>{new Date(doc.uploaded_at).toLocaleDateString()}</td></tr>)}</tbody></table></div>:<Empty title="Your documents will appear here">Upload project proposals, meeting notes, progress updates, or task lists to get started.</Empty>}
