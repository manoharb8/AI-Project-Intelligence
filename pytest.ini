[pytest]
pythonpath = backend .
testpaths = backend/tests tests
markers =
    integration: requires actual WordLlama model and ChromaDB
    live: launches the real backend and frontend on ports 8000 and 5173
